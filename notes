Functions to remember

for M ^2 model
np.sign
np.square

np.stats.pearsonr(TMSCORES,EBETTER)[0] (returns a tuple)

TMSCORE[np.argmin(EBETTERS)]

Profiling:
    python -m cProfile -o outputz <scriptname> modelComplexity fileout generations

    load in interpreter with:
    import pstats
    p = pstats.Stats('model1Stats')
